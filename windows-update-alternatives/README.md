# Windows下的update-alternatives

> 第一次接触update-alternatives就想搞一个windows版的，最近刚好有空  


想了好久还是打算用多年没用用过的C++写，也算是复习复习，还有就是要是用比较熟悉的python写就不能用这个工具控制python的版本，其他语言类似，要打包exe好像C++比较合适（go也很合适。。）


# 一些设想
```
// 尽可能的还原update-alternatives，尽量做到一摸一样
// 感觉原理不难，先自己实现着，不行再看一下原工具的代码
// 首先设置环境变量到某个链接路径(希望可以在install中实现)
// 要有一个配置文件，里面标清注册了那些应用，目前是什么版本，一共有那些版本
// 运行文件，接收命令行参数和原版工具一致，有初始注册组件(install)，删除组件(remove)和配置组件(config)功能

```